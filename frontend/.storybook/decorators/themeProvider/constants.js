const DECORATOR_NAME = 'themeProvider';

const ADDON_ID = 'themeProvider';

const ADDON_NAME = 'Theme Provider';

const THEMES = {
  LIGHT: 'light',
  DARK: 'dark',
};

const ADDON_INITIAL_STATE = THEMES.LIGHT;

const SET_THEME_EVENT = 'setTheme';

export {
  ADDON_ID,
  ADDON_INITIAL_STATE,
  DECORATOR_NAME,
  ADDON_NAME,
  THEMES,
  SET_THEME_EVENT,
}
